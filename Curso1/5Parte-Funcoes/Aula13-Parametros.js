let numa = 12;
let numb = 15;

function soma(num1=0, num2 =0){
    // É possível definir valores padrões para as funções. Caso não recebam esses parâmetros, utilizarão os valores padrões
    return num1+num2;
    // o retorno é o retorno da função. Ela disponibiliza dados para outras partes do programa, por isso é retornada.
}

console.log(soma(numa));

// Parâmetro e Argumento
// São a mesma coisa

// Parâmetros e funções possuem uma ordem!