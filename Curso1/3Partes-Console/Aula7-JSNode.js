// JavaScript é uma linguagem dinâmica. Uma linguagem é dinâmicamente tipada quando não é necessário declarar o tipo. Ele aceita a criação de variáveis de tipo number e depois trocar p/ string. Ele vê qual é o tipo e infere isso, logo, é dinâmico.

// JS é do tipo untyped. Não precisa declarar tipo de variável!
let minhaVar = 567;
minhaVar = "oi";

// Multiparadigma
// Resolver o mesmo problema de várias formas diferentes. Também é possível utilizá-lo como uniparadigma. 

// O que é ES6 / ES2015?
// é a versão do JavaScript de 2015. É muito marcante, pois houve atualizações anuais e é a versão mais atualizada, funcionalidades novas e permitiu o uso no dia-a-dia.

// Linguagem interpretada
// Existem interpretadas e compiladas.
// Interpretadas: Escreve o código -> passa para o programa -> lê enquanto lê
// Compiladas: Escreve -> passa para o compilador que transforma em linguagem de máqina -> a gente lê a linguagem de máquina. Rodam mais rápido e passam por compilador.

// O que é NodeJS?
// O JS é interpretado e utiliza um interpretador, pode ser o node ou o navegador. 
// Node faz isso no lado do back-end.
// Node é uma ferramenta desenvolvida em 2010 para utilizar fora do interpretador padrão (navegador)
